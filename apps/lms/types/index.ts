/**
 * LMS app type definitions.
 * Re-export from here for a single import path: import type { RouteContext } from "@/types";
 */
export type {
  RouteContext,
  RouteContextNoParams,
  GetRouteHandler,
  PostRouteHandler,
  PutRouteHandler,
  PatchRouteHandler,
  DeleteRouteHandler,
} from "./api";
